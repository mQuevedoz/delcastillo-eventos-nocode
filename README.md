📝 Descripción

Infoweb Castillo es un sistema web para la gestión de información y eventos de la empresa.
Está dividido en frontend (interfaz de usuario con React) y backend (API con Node/Express y base de datos MongoDB).

🔹 Frontend (React + Vite)
cd frontend
npm install
npm run dev

🔹 Backend (Node + Express + MongoDB)
cd backend
npm install
npm run dev

🔹 Crear Rama Personal (Estando en main para que avancen desde lo que esta en esa rama)
git checkout -b TUNOMBRE

🔹 Subir Cambios (Estando en tu rama, NO ESTAR EN MAIN)
git add .
git commit -m "mensaje de avance (escriben lo que quieran)"
git push origin HEAD

.